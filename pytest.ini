[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# video_widget_manual_test.py 需要 QApplication（在 Windows + git-bash + offscreen
# 环境下会卡），命名不以 test_ 开头，默认不被收集；手动运行：
#     QT_QPA_PLATFORM=offscreen python tests/video_widget_manual_test.py
